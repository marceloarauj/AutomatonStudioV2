import 'package:flutter/material.dart';

class NewProjectView extends StatelessWidget {
  const NewProjectView({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('NewProject')
    );
  }
}