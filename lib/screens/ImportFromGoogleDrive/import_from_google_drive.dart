import 'package:flutter/material.dart';

class ImportFromGoogleDriveView extends StatelessWidget {
  const ImportFromGoogleDriveView({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('ImportFromGoogleDrive')
    );
  }
}